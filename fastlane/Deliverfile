# generated by storekit — edit store.config.json, not this file

# Metadata and screenshots come from the generated trees; nothing is typed twice.
app_identifier("com.levelhard.cadentia")
primary_category("")
secondary_category("")

# The gate. Everything is uploaded and left sitting in "Ready for Review" —
# a human still presses Submit, on purpose.
submit_for_review(false)
automatic_release(false)
force(true)              # skip the HTML preview prompt; preflight already ran
precheck_include_in_app_purchases(false)
overwrite_screenshots(true)
run_precheck_before_submit(true)

# classificação etária: respondida no console. O questionário da Apple mudou
# em 2026 (perguntas novas de redes sociais) e declarar conteúdo errado é pior
# que não declarar. storekit só emite este arquivo se apple.ageRating existir.
