<?xml version="1.0" encoding="utf-8"?>
<!--
  SÓ NA BUILD DEBUG: libera HTTP em claro para o host do emulador (10.0.2.2),
  para o QA servir o `separator.onnx` de um `python3 -m http.server` no Mac e
  exercitar o download de verdade (Range, SHA-256, progresso) sem publicar o
  modelo em lugar nenhum. A build de release não tem este arquivo e continua
  recusando tráfego em claro.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
